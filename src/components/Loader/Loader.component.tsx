import React from "react";
import { LoaderContainer, Spinner } from "./Loader.styles";

const Loader = () => {
  return (
    <LoaderContainer>
      <Spinner />
    </LoaderContainer>
  );
};

export default Loader;
